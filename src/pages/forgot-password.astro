---
import Layout from "@/layouts/Layout.astro";
---

<Layout title="Zapomniałeś hasła? - 10xCards">
  <div
    class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 via-white to-purple-50 py-12 px-4 sm:px-6 lg:px-8"
  >
    <div class="w-full max-w-md">
      {/* Logo/Brand */}
      <div class="text-center mb-8">
        <a href="/" class="inline-block">
          <span class="text-3xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
            10xCards
          </span>
        </a>
      </div>

      {/* Password Reset Form */}
      <div class="w-full max-w-md">
        <div class="text-center mb-8">
          <h1 class="text-3xl font-bold text-gray-900 mb-2">Zapomniałeś hasła?</h1>
          <p class="text-gray-600">Wpisz swój adres email, a wyślemy Ci link do resetowania hasła</p>
        </div>

        <div class="bg-white rounded-2xl shadow-lg border border-gray-200 p-8">
          <form class="space-y-6">
            {/* Email Field */}
            <div>
              <label for="email" class="block text-sm font-medium text-gray-700 mb-2"> Email </label>
              <input
                id="email"
                type="email"
                class="w-full px-4 py-3 border border-gray-300 bg-white rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/50 transition-all"
                placeholder="twoj@email.com"
                required
              />
            </div>

            {/* Submit Button */}
            <button
              type="submit"
              class="w-full bg-primary text-primary-foreground hover:bg-primary/90 font-medium py-3 px-4 rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2"
            >
              Wyślij link resetujący
            </button>
          </form>

          {/* Info box */}
          <div class="mt-6 p-4 bg-blue-50 border border-blue-200 rounded-lg">
            <div class="flex items-start gap-3">
              <svg
                class="w-5 h-5 text-blue-600 flex-shrink-0 mt-0.5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <p class="text-sm text-blue-800">
                Otrzymasz email z linkiem do resetowania hasła. Link będzie ważny przez 1 godzinę.
              </p>
            </div>
          </div>
        </div>

        {/* Back to login */}
        <div class="mt-6 text-center text-sm text-gray-600">
          Pamiętasz hasło?{" "}
          <a href="/login" class="font-medium text-primary hover:text-primary/90 transition-colors">
            Wróć do logowania
          </a>
        </div>
      </div>
    </div>
  </div>
</Layout>
